version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taichung-urban-renewal
    ports:
      - "5173:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
